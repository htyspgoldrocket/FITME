# 환경 변수 — API 키는 절대 커밋 금지 (규칙 10)
.env
.env.*
server/.env

# 의존성
node_modules/
server/__pycache__/
**/__pycache__/
server/venv/
.venv/
.pytest_cache/

# 빌드 산출물
dist/
dist-ssr/
*.local

# 에디터/OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# 테스트 픽스처 — 개인 신체 사진·실측값은 로컬에만 보관 (README.md만 커밋)
server/tests/fixtures/*
!server/tests/fixtures/README.md

# E2E 가짜 카메라 영상 — tests/gen_fake_camera.py로 생성 (바이너리라 커밋 제외)
tests/fake-marker.y4m

# 의류 스펙 SQLite 캐시 — 로컬 생성물 (services/clothing_store.py)
server/data/clothing_cache.sqlite
server/data/usage.sqlite

# 로그
*.log
npm-debug.log*
